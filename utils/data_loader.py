import pandas as pd
import os

def load_data(data_path='ml-100k'):
    """
    Charge les données MovieLens depuis le dossier spécifié.
    Retourne un dictionnaire de DataFrames.
    """
    # Exemple de chargement (à adapter selon les fichiers réels)
    # columns = ['user_id', 'item_id', 'rating', 'timestamp']
    # ratings = pd.read_csv(os.path.join(data_path, 'u.data'), sep='\t', names=columns)
    
    print(f"Chargement des données depuis {data_path}...")
    return {}
